import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vacationtracker.pro',
  appName: 'Vacation Tracker Pro',
  webDir: 'dist'
};

export default config;
